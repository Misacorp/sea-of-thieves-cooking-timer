{
  "name": "sea-of-thieves-cooking-timer-server",
  "version": "1.0.0",
  "description": "Socket.io server for the Sea of Thieves Cooking Timer.",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && npm run serve",
    "build": "babel lib -d dist",
    "serve": "node dist/index.js",
    "watch": "nodemon --watch lib --exec npm run start"
  },
  "author": "Misacorp",
  "license": "MIT",
  "dependencies": {
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/core": "^7.4.5",
    "@babel/preset-env": "^7.4.5",
    "nodemon": "^1.19.0"
  }
}
